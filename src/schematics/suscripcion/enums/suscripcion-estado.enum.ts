/** Alineado con `SubscriptionStatus` en app-venta-flash (`lib/session-types.ts`). */
export enum SuscripcionEstado {
  ACTIVA = 'active',
  EXPIRADA = 'expired',
  GRACIA = 'grace',
  BLOQUEADA = 'blocked',
}
